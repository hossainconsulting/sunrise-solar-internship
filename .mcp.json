{
  "mcpServers": {
    "salesforce-dx": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@salesforce/mcp",
        "--orgs",
        "sunrise",
        "--toolsets",
        "data,metadata,testing",
        "--no-telemetry"
      ]
    }
  }
}
